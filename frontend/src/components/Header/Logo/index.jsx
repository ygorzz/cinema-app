import styled from "styled-components";

const Title = styled.h2`
    font-size: 36px;
    cursor: pointer;
`

function Logo(){
    return (
        <Title>CinemaAPI</Title>
    )
}

export default Logo;